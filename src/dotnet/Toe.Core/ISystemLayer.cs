namespace Toe.Core
{
	/// <summary>
	/// Set of components at system.
	/// One system can have multiple layers, but there is only one layer in most situations.
	/// </summary>
	public interface ISystemLayer
	{
		
	}
}